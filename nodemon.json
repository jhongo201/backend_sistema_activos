{
  "watch": ["src"],
  "ext": "ts",
  "ignore": ["src/**/*.spec.ts"],
  "exec": "node --loader ts-node/esm --no-warnings src/app.ts || ts-node src/app.ts",
  "env": {
    "NODE_ENV": "development"
  }
}
